/** Günlük mod kaydı — `app/rengi-hatirla/page.tsx` ile aynı anahtar. */
export const LS_RENGI_DAILY = 'renkle-rengi-hatirla-daily'

export { getLocalDayId as dayKey, calendarDaysBetween } from '@/lib/streak'
